#
# example config file for the signer engine
#
#

# for every pkcs11 module that is used:
# pkcs11_module: <name> <module path> <pin>
# this should probably be moved to the same location the kasp uses
pkcs11_token: test_token ../../softHSM/src/lib/.libs/libsofthsm.so 1111

# these dirs are used atm (no defaults yet)

# unsigned zone files
zone_input_dir: unsigned_zones
# signed zone files for the server to use
zone_output_dir: signed_zones
# kasp zone configuration data
zone_config_dir: zone_config
# place to drop some intermediate data
zone_tmp_dir: tmp

# we can probably remove this later once install paths are present etc.
tools_dir: ../../signer_tools
