$Id$

This code is to be considered as work-in-progress.
It may or may not compile correctly.
It may or may not work correctly.

Using Botan 1.8.0

SoftHSM is not thread safe.... but will be fixed
Check if any memory leeks must be fixed


chmod 744 autogen.sh
./autogen.sh
./configure (--with-botan=DIR where DIR is the path to the Botan lib if other than default)
make
sudo make install
